---
description: Run the full content generation pipeline (Keyword -> Researcher -> Generator -> Publisher)
---
To run the full content generation pipeline, execute the following command.
You can optionally provide a keyword to override the default one in `data/keywords.json`.

1. Run the pipeline
```bash
node automation/pipeline/index.js --keyword "Your Keyword Here"
```
